你可以使用子代理。话说你的plan里面包含testament和levelupper了吗？这个也要写一下吧。顺便可以再加两个extention，一个是napcatqq的，你要自己去其仓库阅读文档，相当于包装一层。我希望能够支持全量qq功能，但是要考虑到提示词长度（premise），同时我不希望其所有事件一条一条上报，浪费token，考虑能否动态注入或者可查询一类的。我希望面向llm module或者其他时能够像一个真正的qq一样，类似一个状态机一类的，可以有消息通知，然后可以主动查看有哪些新消息，进入某某群聊一类的。这样包装一下，建议你找一下有没有现有的这类项目。如果是mcp的话也可以参考，但是我希望以extention的形式，不希望直接放入llm module里面。extention就配置通讯吧，可以再额外加配置，比如禁止在某某群说话。反正我希望能够像一个交互性强的extention，而不是一个来回通信，发送事件的一个东西。不过如果要简单一点的话，甚至可以用一个page把一个简易的extention和一个llm module连起来，然后给llm module写一些提示词，告诉其用来发送通知，相当于对其它module语义中转一下也行。你自己拟定一个好的plan。
第二个extention是我设想的一个纯功能的filter。这个就比较具体了，你应该能更好理解。就是通过premise提示词，让能够输入到它的module，在extra_body，或者使用mothod，你自己定。传一个0-1000的数字。每次输出都有，如果之前传过但是后面没有的就默认保持不变。然后这个module维护一下，如果有的source的block从来没有过这个数那就忽略掉（比如一些非llm的模块，但是并不是说非llm的模块不能有，注意这个架构里面module之间本质是没有特定关系的，所以这么说，就是并不是面向特定module的，只是一个通用功能）。就维护一下这些有的的source的这个值。然后每次调用这个filter的时候呢，看谁的这个数离mean的2/3最近，那么将重放这个source的block给下游，其他的就抛弃掉（我想的话这个地方最好不用forward？因为filter可能嵌套？所以直接重放一下是最好的，注意要去掉一些部分哦，比如blockid这些外部的）。因为可能收到很多次，所以我想的是滑动平均一下，就是（1-beta)维护的值乘上beta新的这样子。可能需要无偏纠正一下。大概补充这两个extention。你自己给自己设定goal，完成整个plan。需要进行review，对抗审查，模拟各种操作最后交付。
