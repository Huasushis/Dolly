# 新上传材料审阅记录

本页是来源审阅记录，不是规范条款。

## HTML 结构结论

`project_sources/03-dolly-2026-08-10.html` 的页面标题为
“dolly - 模型介绍”，正文恰好包含四条消息：用户询问模型、助手回答、用户给出
项目需求及 Memory 澄清、助手给出完整工程规划。助手文本明确自述为
“GPT-5.6 Pro”。静态导出可以证明页面显示了这段自述，不能独立证明服务端路由
记录，因此仓库不以模型身份赋予内容权威性。

最后一条助手回复是完整稿，而不是中途截断：机械提取的 Markdown 有 29 个一级
章节，实施路线从 Phase 0 延伸至 Phase 10，并以“Dolly v1 的完成标准”及总结段
正常结束。文件中没有旧稿标识 `Dolly Specification v1.0`。

## 与排除材料的区别

这份 HTML 不是先前明确排除的《项目规划与技术栈建议》旧稿。排除规则仍然有效：
旧稿没有进入本仓库，也不能用于填补新材料的遗漏。

## 内容评价

新回复是一份质量较高、覆盖面广的 architecture plan/spec draft：它正确识别了
稳定 Core、进程隔离 Extension、协议、恢复、配置事务、Assets、模型适配、Memory
实验与工程化路线。它仍不是能让两个独立团队必然实现出相同可观察行为的最终
规范，主要缺口包括精确状态机、事务崩溃点、线级 Schema、幂等与 unknown-outcome
语义、授权投影、错误闭包和可执行测试向量。

本仓库据此把助手回复保留为非规范性工程基线，再以所有者原话为更高优先级，
将上述缺口冻结为正式条款、Schema、协议示例和测试向量。具体顺序与校验值见
[Source manifest](SOURCE-MANIFEST.md)。

## 最终采用顺序

1. HTML 中最新的所有者 Memory 澄清；
2. `01-dolly_new.txt`；
3. `02-newnew.txt` 中不冲突的补充；
4. 新 HTML 中的助手工程规划；
5. 受来源约束、由测试与 ADR 支持的工程细化。

